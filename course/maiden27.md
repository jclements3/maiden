# maiden27 — Metrics & the gate [desk]

**Sprint goal.** Finish `maiden.validate`: rollups, the ≥8-of-10 campaign
gate, confidence-band extraction, and D8-schema output behind the
`maiden validate` CLI — then run VT-17 for real against the twin.

**Depends on.** maiden26 (align + residuals). Feeds maiden28 (CI calls
the gate) and maiden55–57 (`bands.json` → the pilot report).

**Read first.** lesson14.md — *Concepts* "Step 4 — rollups, defined
precisely" and "Steps 5–6 — the gate and the bands", then *Build* (the
`FlightMetrics` skeleton and output contracts) and *Verify*.

## Tasks

- [ ] `rollup(res, segments)`: scalar-norm pos RMS, pos p95, vel RMS,
      continuity — per flight and per maneuver segment. Segment labels
      arrive with maiden51; until then everything is one `ALL` segment,
      but the optional segment-table interface exists now. Keep per-axis
      breakdowns in the JSON (free, and they localize problems: U bias
      smells like survey, E/N lag smells like sync).
- [ ] `FlightMetrics` dataclass per the lesson skeleton, including the
      `sync` column ("ok" / "degraded(+8.2ms)").
- [ ] `gate(flights, need=8, of=10)` as a pure function — CI calls it.
      Unit tests: 8/10 passes, 7/10 fails, and the boundary case where
      one flight's continuity alone fails it.
- [ ] `bands(all_res)` → position p95, speed p95, per-maneuver score-band
      inputs, persisted to `results/validate/bands.json`.
- [ ] Output writers: `report.json` + `report.md` — Accuracy table
      columns exactly `Flight | Aircraft | Sequence | Pos RMS (m) |
      Pos p95 (m) | Vel RMS (m/s) | Continuity | Sync | Pass`;
      By-maneuver table exactly `Maneuver | n | Pos RMS (m) |
      Vel RMS (m/s) | Continuity | Notes` with D8's maneuver rows + ALL.
- [ ] CLI: `maiden validate --session DIR` and
      `maiden validate --campaign DIR` (combined tables + gate verdict).
- [ ] **Schema check**: diff your markdown headers column-for-column
      against the D8 HTML in `docs/` — paste-into-D8 is the acceptance
      test for the format.
- [ ] **Known-noise consistency run (VT-17)**: imperfect twin through the
      real pipeline; measured pos/vel RMS vs the values predicted from
      injected σ_θ/σ_vr through the D3 geometry — agreement within ~25%
      (perfect agreement would be suspicious). Results to
      `results/VT-17/`.

## Done when

- `maiden validate --session <twin dir>` writes `report.json` +
  `report.md` in D8's schemas with a gate verdict.
- `gate()` unit tests pass including boundary cases; the known-noise
  consistency evidence is committed under `results/VT-17/`.
- `bands.json` exists from a twin campaign run — the interface maiden55
  consumes.

## Doc trace

D7 §2 steps 4–6 (step 7, judge correlation, is maiden64); VT-10/11/12
scoring machinery (field-bound); VT-17 executed, evidence filed; D8
Accuracy / By-maneuver / §Confidence bands generated by tool, making the
template's "generated by maiden.validate" note true.
